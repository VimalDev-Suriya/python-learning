# Strings:

Collection of characters are called Strings.

1. `len()`
2. `str.count("")`
3. split and strip
4. find()
5. String concatenation by just using "+" similar to JS
6. isAlpha() - returns true is the string is a aplhabet
7. isDigit - returns true if strings are numbers